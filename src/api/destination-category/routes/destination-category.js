'use strict';

/**
 * destination-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::destination-category.destination-category');
